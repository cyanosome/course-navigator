[project]
name = "course-backend-workspace"
version = "0.1.0"
description = "Course Navigator Backend Workspace"
requires-python = ">=3.14"
dependencies = []

[tool.uv.workspace]
members = [
    "core",
    "api",
    "ingestion",
    "agent",
]
